**This is my config for:**  
**-Niri Noctalia-qs (plugin:clipper,screenshot,keybindsheatsheet)**  
**-Zed editor (compile&run by F5 keybind,only compile by Shift+F5)**  
   
**Ps:I take niri config from cachyos niri noctalia**  

**Installation**

Clone repo on Nixos branch:

```bash
git clone --branch Nixos --single-branch https://github.com/kizuto2006/BackupLinux.git
```

Go to folder and run shell:

```bash
cd BackupLinux
chmod +x nix_build.sh
./nix_build.sh
```
Quick install:

```bash
git clone --branch Nixos --single-branch \
  https://github.com/kizuto2006/BackupLinux.git

cd BackupLinux
chmod +x nix_build.sh
./nix_build.sh
```
